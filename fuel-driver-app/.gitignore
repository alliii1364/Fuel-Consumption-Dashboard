# Capacitor driver app — commit source/config only, not build outputs.
node_modules/

# Firebase client config (treated as secret, like the backend's)
android/app/google-services.json
www/
dist/
*.apk

# Bundled web export (regenerated by `npm run sync:web`)
android/app/src/main/assets/public/

# Android build artifacts
android/build/
android/app/build/
android/.gradle/
android/captures/
android/local.properties
android/.idea/
android/app/release/
.gradle/
